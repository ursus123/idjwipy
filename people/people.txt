CREATE TABLE `people` (
  `id` uuid NOT NULL,
  `names_id` uuid NOT NULL,
  `types_id` uuid NOT NULL,
  `contacts_id` uuid NOT NULL,
  `addresses_id` uuid NOT NULL,
  `enterprise_id` uuid NOT,
  `date_of_birth` DATE NOT NULL,
  `height` VARCHAR (50),
  `weight` VARCHAR (50),
  `hair_color` VARCHAR (50),
  `eye_color` VARCHAR (50),
  `job_id` uuid,
  `hire_date` DATE NOT NULL,
  `education_id` uuid,
  `gender` VARCHAR (50) NOT NULL,
  `marital_status` VARCHAR (50) NOT NULL,
  `reference_id ` uuid,
  `salary` INTEGER,
  `company_id` uuid,
  `owner_names_id` uuid,
  `last_contacted` DATE,
  `created_at` DATE NOT NULL,
  `created_by_names_id` uuid,
  `updated_at` DATE,
  `updated_by_names_id`
  KEY `PRIMARY KEY` (`id`),
  KEY `FOREIGN KEY` (`names_id`,
                    `types_id`,
                    `contacts_id`,
                    `addresses_id`,
                    `job_id`,
                    `education_id`,
                    `reference_id `,
                    `enterprise_id`,
                    `owner_names_id`)
);
